rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Any signed-in user can read and write bill photos in the shared workspace.
    match /workspaces/{ws}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
